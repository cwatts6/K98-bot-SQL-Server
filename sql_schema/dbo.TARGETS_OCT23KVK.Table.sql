SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[TARGETS_OCT23KVK]') AND type in (N'U'))
BEGIN
CREATE TABLE [dbo].[TARGETS_OCT23KVK](
	[GovernorID] [float] NOT NULL,
	[KILL_TARGET] [int] NULL,
	[DEAD_TARGET] [float] NULL
) ON [PRIMARY]
END
